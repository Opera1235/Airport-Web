# Airport Flight Management Admin - Web Version

这是一个机场航班管理系统的静态网页版本，可以在 GitHub Pages 上轻松部署。

## 功能特性

- ✅ 航班列表查看
- ✅ 航班筛选（类型、状态、航空公司、航班号搜索）
- ✅ 航班排序（按计划时间）
- ✅ 添加新航班
- ✅ 编辑现有航班
- ✅ 删除航班
- ✅ 数据持久化（使用 localStorage）

## 技术栈

- Vue 3
- Vue Router
- Tailwind CSS
- Vite

## 本地开发

### 安装依赖

```bash
npm install
```

### 启动开发服务器

```bash
npm run dev
```

开发服务器将在 `http://localhost:5173` 运行。

### 构建生产版本

```bash
npm run build
```

构建产物将输出到 `dist` 目录。

### 预览生产构建

```bash
npm run preview
```

## 部署到 GitHub Pages

📖 **详细部署指南请查看：[DEPLOYMENT_GUIDE.md](./DEPLOYMENT_GUIDE.md)**

### 快速开始

1. **创建 GitHub 仓库**
   - 在 GitHub 上创建新仓库（建议命名为 `airport-web`）

2. **推送代码到 GitHub**
   ```bash
   git init
   git add .
   git commit -m "Initial commit"
   git remote add origin https://github.com/YOUR_USERNAME/airport-web.git
   git branch -M main
   git push -u origin main
   ```

3. **启用 GitHub Pages**
   - 进入仓库 Settings → Pages
   - Source 选择 "GitHub Actions"
   - 等待部署完成（1-3分钟）

4. **访问网站**
   - 网址格式：`https://YOUR_USERNAME.github.io/airport-web/`
   - 例如：`https://leung.github.io/airport-web/`

### 方法 1: 使用 GitHub Actions（推荐）

1. 将代码推送到 GitHub 仓库
2. 在仓库设置中启用 GitHub Pages，选择 "GitHub Actions" 作为源
3. 每次推送到 `main` 分支时，GitHub Actions 会自动构建并部署

### 方法 2: 手动部署

1. 构建项目：
   ```bash
   npm run build
   ```

2. 如果您的仓库名不是根路径，需要修改 `vite.config.js` 中的 `base` 配置：
   ```js
   base: '/您的仓库名/',
   ```

3. 将 `dist` 目录的内容推送到 `gh-pages` 分支，或使用 GitHub Pages 设置指向 `dist` 目录。

## 数据存储

本应用使用浏览器的 `localStorage` 来存储航班数据。这意味着：
- 数据存储在用户的浏览器中
- 不同浏览器/设备之间的数据不会同步
- 清除浏览器数据会删除所有航班记录

## 项目结构

```
Airport-Web/
├── src/
│   ├── components/      # Vue 组件
│   │   ├── FlightTable.vue
│   │   ├── FlightFilterBar.vue
│   │   └── FlightForm.vue
│   ├── views/           # 页面视图
│   │   └── FlightListView.vue
│   ├── services/        # 服务层
│   │   ├── api.js       # API 接口（兼容层）
│   │   └── flightStore.js  # 数据存储服务
│   ├── data/            # 初始数据
│   │   └── flights.js
│   ├── utils/           # 工具函数
│   │   └── validators.js
│   ├── router/          # 路由配置
│   │   └── index.js
│   ├── App.vue
│   ├── main.js
│   └── style.css
├── index.html
├── package.json
├── vite.config.js
└── README.md
```

## 与原版本的区别

- **后端功能已转换为前端逻辑**：所有 API 调用现在使用 `localStorage` 进行数据管理
- **无需服务器**：完全静态，可以在任何静态托管服务上运行
- **数据持久化**：使用浏览器本地存储，数据在页面刷新后仍然保留

## 许可证

ISC

